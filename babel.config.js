const plugins = ['@babel/plugin-proposal-class-properties'];

const presets = [
  '@babel/preset-typescript',
  '@babel/preset-react',
  '@babel/preset-env',
];

module.exports = {
  plugins,
  presets,
};
